branches:
  only:
  - master
  - /^v[\d.]+$/
language: julia
julia:
  - 1.0
codecov: true
after_success:
  - julia -e --project=docs/ 'import Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'
  - julia --project=docs/ docs/make.jl
